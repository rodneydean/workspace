import { View, Text } from 'react-native';

export default function Activity() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-on-surface font-bold">Activity Screen</Text>
    </View>
  );
}
